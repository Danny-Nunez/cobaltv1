{
    "name": "cobalt-api-deployment",
    "version": "1.0.0",
    "private": true,
    "engines": {
        "node": ">=18.0.0"
    },
    "scripts": {
        "start": "node src/cobalt.js",
        "build": "cd api && pnpm install --no-frozen-lockfile"
    }
}